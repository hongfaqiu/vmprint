export function getDictionaryHyphenBreaks(word: string, lang: string): number[] {
    // Dictionary hyphenation has been removed to keep the core engine fully
    // environment-agnostic.
    return [];
}
